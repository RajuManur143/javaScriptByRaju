// Syntax of immediate ivoke function (function f_name{})   //invoke()
// Syntax () ()
(function area_of_circle() {
    let r = 10;
    const PI = 3.142;
    let res = PI * r * r;
    console.log(res);
})

// invoke
();